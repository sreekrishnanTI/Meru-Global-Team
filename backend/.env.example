# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# You can use a full connection string or individual credentials
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/accardio
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=accardio

# Security & JWT Secret
JWT_SECRET=super-secret-accardio-jwt-key-2026

# CORS Origin (Frontend URL)
FRONTEND_URL=http://localhost:3000
